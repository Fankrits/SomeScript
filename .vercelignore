# Dependencies & Build artifacts
node_modules
.next
.turbo
.git
.eve
.output
dist
build

# App workspaces ignored when deploying web landing/dashboard
apps/editor
apps/compiler
apps/docs
# Deploys on Railway (railway.json + Dockerfile), never Vercel. Without this it
# stays in `turbo run build`'s scope and fails: server.ts imports
# ../editor/lib/storage, which apps/editor above has already stripped.
apps/collaboration
workspaces
projects
bin

# Environment & local files
.env*.local
*.log
